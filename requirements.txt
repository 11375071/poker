# Poker AI project (6-max NLHE with OpenSpiel)
# From project root: source .venv/bin/activate && pip install -r requirements.txt  (Linux/macOS)
# Or: .venv\Scripts\activate ; pip install -r requirements.txt  (Windows)

# OpenSpiel: from PyPI (recommended on Linux) or local build: pip install -e ./open_spiel
open-spiel>=1.0.0
numpy>=1.21.5
absl-py>=0.10.0
scipy>=1.10.1
ml-collections>=0.1.1

# 6-max 环境（需 Python 3.11+）。完整安装请运行: scripts/setup_venv_6max.sh 或 pip install 'pokerkit>=0.4.0'
# pokerkit>=0.4.0
